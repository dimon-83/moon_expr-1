# Examples

- basic_eval.mbt: arithmetic expression evaluation with variables
- builtins_example.mbt: using builtin functions like len

Run a single example test:

```
moon test examples/basic_eval.mbt -q
moon test examples/builtins_example.mbt -q
```
